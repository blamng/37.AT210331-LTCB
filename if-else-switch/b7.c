#include <stdio.h>
int main() {
   double dtb;
   scanf("%lf", &dtb);
   if(n < 5)
       printf("Y");
   else if(n >= 5 && n < 7)
       printf("TB");
   else if(n >= 7 && n < 8)
       printf("K");
   else if(n >= 8 && n < 9)
       printf("G");
   else
       printf("XS");
   return 0;
}
